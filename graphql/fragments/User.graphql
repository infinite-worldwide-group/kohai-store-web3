fragment UserSession on User {
  id
  email
  emailVerified
  emailVerifiedAt
  updatedAt
  walletAddress

  # VIP Tier fields
  tier
  tierName
  discountPercent
  kohaiBalance
  tierBadge
  tierStyle
}
